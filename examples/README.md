# Examples

- `default` deploys only the access connector with the module defaults.
- `large-parameter-set` exercises the full supported module surface and shows a realistic storage RBAC grant on the connector's system-assigned identity.
- `waf-aligned` shows the recommended locked-down baseline for a production-oriented connector.
